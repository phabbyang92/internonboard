export enum OnboardingStatus {
  Candidate = 'candidate',
  PendingOnboarding = 'pending_onboarding',
  Onboarded = 'onboarded',
}

export enum AttachmentType {
  Resume = 'resume',
  IdCard = 'id_card',
  Other = 'other',
}
